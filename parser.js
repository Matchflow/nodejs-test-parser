/**
 * @param {Array<Array>} _data - file data
 * @return {{data: Array<Object>, type: string}}
 */
const parser = (_data) => {
  // todo Place for parsing code for data received from the reader

  return {
    data: [],
    type: '',
  };
};

module.exports = parser;
